# v0.2 实机验证 · 2026-09-12

主代理亲自执行，无其他 agent 代跑。真实个人记录只留本地忽略目录，本文件只存结果与限制。

## 已验证

- Windows：官方安装命令注册 Cordis bundle，安装文件逐个字节核对；已有官方 DSH 与材料插件保留。已有 Skill 和 profile 配置自动备份。Node 后台启动、正常认证、原生 API 与 /study 页面成功。
- Windows 首轮自动背景导入：18.269 秒生成，18.721 秒显示回执；发现旧快照与相对路径处理问题后修正。第二轮：23.588 秒生成，23.915 秒显示回执，23 条有原文的事实；独立检查后停用重复、冲突等 6 条，17 条启用，coverage 仍为 partial。
- Windows API 提问（Max）：自动注入 profile、全新原生会话，5.427 秒生成、5.851 秒显示回执。
- Windows 页面真实点击发送（Max）：6.420 秒生成，**7.001 秒发送到完整显示**。发送时没有手工点击 DSH 的会话选择。派工/来源/完整回答/每轮指标均落盘。
- 同会话验收修正：3.747 秒生成；再次精简 3.512 秒，375 字符最终解释通过 Codex 实读验收。前两轮内容问题和 changes_requested 都保留。各轮时间不是整个多轮人工审阅过程的总时间。
- 原生模型工具：DSH 实际调用 study_connection_status 和 study_personal_context，各一次。首测因 undefined 字段违反 lossless JSON 输出约束失败；修正后 3.143 秒生成并返回正确状态与课程，两次 tool/call、两次 tool/result 均在原生日志中。
- Windows 正常入口 /#study 自动进入 /study，使用当前已有认证 Cookie；无需修改核心页面源码。
- 认证边界：未登录 401、错误 Origin 403、路径穿越探针 404。非 JSON body、输入限制、来源过滤、同 ID 防重、停用事实和回执不确定的日志恢复均有检查。
- macOS ARM：通过用户授权的 Tailscale/SSH 安装官方 DSH、插件及 Skill，在桌面子工作区实跑。修复 Homebrew npm 发现、缺 pnpm，以及插件热加载抢跑自动导入的问题。
- Mac 全新工作区自动导入（Max）：102.806 秒，24 条事实，使用显式导出的本人规则和学校资料；启动期间两次旧测试被重启中断的日志保留。后续真实背景提问（Max）：2.458 秒生成，课程名称与假设例子正确。
- 自动化回归：9 项 Node 测试通过；两个 Skill 的正式验证器通过；PowerShell bootstrap 解析通过。没有把单元测试代替实机模型和页面证据。

## 尚未证明或仍有不足

Mac 窗口未目视确认，故该侧 display 为 not_observed。Windows 和 Mac 的“没有 Node 时自动下载”分支已写 SHA256 校验，尚未在无 Node 机器实跑。当前交付为启动脚本和本地页面，没有签名 exe/dmg。

云端历史没有自动读取，来源覆盖部分完成。机器只能校验原文存在，事实语义、冲突与时效仍需要验收。首轮与后续轮耗时差别明显，不能承诺任意初始化或任务 30 秒。网页显示采用文字与完整日志，图解与精细视觉设计不在此次实现范围。
