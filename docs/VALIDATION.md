# 验证记录

2026-09-12，在新的独立项目与本机官方 DSH 环境执行。凭证、资料原文和完整本机报告留在被忽略的 `.env`、`state/`、`runtime/`，不随 Git 交付。

## 独立后端与真实来源

新项目根凭证通过一次真实 Canvas 课程列表 GET，认证有效。只从源项目安全复用 Canvas 地址与 token，没有搬入旧课程缓存、学习记录或其他凭证；资料由新项目重新同步。

配置一门代表课程执行完整同步，197 个 GET/下载请求，3 个模块、61 个目录项。57 项可用：39 个文件、11 个页面、5 条公告、2 条作业说明。仍有 2 个文件、2 个页面不可用；接口错误按原返回保留，不把缺失内容当作成功。

文件列表/页面列表或单项权限受限时，沿模块、首页和正文链接继续读取，取得上述可用资料。实际限制涉及 403/404；失败缓存保留和内容更新替换另用受控测试验证，本轮没有人为修改学校资料或假装观察到教师更新。

通过独立 HTTP API 实际读取一份 1,209,556 字节附件，全文 SHA-256 与同步清单一致；`Range: bytes=0-31` 返回 206 且与文件前 32 字节一致。Canvas 公告正文读取 290 字节。二进制文件返回 `representation=binary`、`text_available=false`、`extraction_status=parser_required`；HTML 正文返回可读文本。

个人本地来源导入仓库合成示例，1 个文件、323 字节，全文 SHA-256 校验与 Range 读取通过；文本表示为 `utf8_text`。未携带内部 API 凭证的资料请求返回 401。完整报告为本机 `runtime/backend-acceptance.json`。

## 当前 DSH 原生执行

CLI 为官方 `@deepseek-ai/dsh 0.1.5-rc.1`；本机依赖中的 `dsh-tools`、`dsh-base` 为 `0.1.5-rc.2`。安装调用官方本地插件命令，在 `web` profile 增加 `@yorhagengyue/dsh-study` bundle；原 base/web 两个 bundle 保留。

插件是 Cordis 的 `study` Service Definition、`python-local` Provider 与七个原生工具 Consumer。最终在实际 DSH 进程（本轮 PID 24812，17:25 北京时间）执行全部七个工具，产生 16 次成功的官方 `tools/result` 事件。完成本地导入/刷新/状态轮询及两类来源目录和读取；Canvas 没有重复同步。同时核对 Python 后端属于该 DSH 父进程，结束旧 DSH 父进程后，旧后端自动退出的生命周期路径通过。

实际读取个人合成示例 323 字节并匹配例句，读取 Canvas 附件前 65,536 字节以及公告正文 290 字节。各读取得到的 SHA-256 与独立后端相同字节范围交叉核对。最终探针结果保存在本机 `runtime/verify-result.json`，每次准备的请求只消费一次，没有外部通用 invoke 接口。

这覆盖当前 DSH 的真实原生工具执行，不包含模型生成。未验证模型自主选择工具、PDF/Word 语义解析、SMU 会话或教学效果。没有打开浏览器或控制电脑界面。

## 受控验证与交付边界

Python 45 项测试通过：资料同步 22、目录/HTML 13、独立后端 10。保留原核心测试并适配明确不迁移的旧快照/网页职责；新增来源隔离、鉴权、Range、UTF-8 分片、本地更新、损坏缓存恢复、并发任务与中断恢复等验证。Node 原生契约 3 项测试通过：使用当前官方 Cordis 与 ToolRuntime 检查参数校验、结构化输出、错误码、取消、注销、后端鉴权与全部七工具的固定本地验收流程。

原网页的 15 分钟自动刷新调度不在新输入插件中运行；通过工具显式刷新。原 `sync.py` 的历史 Markdown 输出、考核分组旧快照、Git pull 和学习日志未迁移。课程、学校和本地目录均可配置，实机本次只验证一个 Canvas 来源的一门课程及一个合成本地来源。

安装更新时发现同路径 tarball 被包管理器缓存，已改为摘要命名的不可变 tarball，并在安装脚本逐文件核对实际安装内容。最终 7 个发布文件与源码一致，读取结果与独立 HTTP 路径的 3 个 SHA-256 全部一致。

实质实现采用功能分支与 draft PR，未自动合并。远端 PRIVATE 可见性、敏感路径忽略及凭证内容检查在交付前核对。卸载使用官方 remove 与 Cordis dispose，注销/子进程退出路径已验证；未在交付前完整卸载当前插件，最终保留已安装可用状态。
