# 这个人（理解）

槽位层。装机时空；由第二步（`protocols/UNDERSTANDING.md`）填，**只写用户点头过的东西**。这里的每一条都是理解不是事实，用户随时能推翻；被推翻的标 `retracted` 保留不删。这个文件和 `UNDERSTANDING/raw/` 永不回传、永不进任何仓库。

<!-- dsh-state -->
```json
{"slot": "understanding", "status": "empty", "ruler_confirmed": false, "confirmed": 0, "pending": 0, "retracted": 0, "cues_open": 0, "cues_off": 0, "corpus_msgs": 0, "last_read_at": null, "user_version": 0, "updated_at": null}
```
<!-- /dsh-state -->

## 尺子（他拿什么量自己）

一句话，用户确认过的。

（空）

## 这个人（已确认的理解）

一页以内。每条：模式在当下这一层的道理、它换来了什么、下一层需要什么；带记录号 `[j-…]` 和日期；含私密来源的标【含私密】。

（空）

## 开口时机

| 触发条件 | 要说的那一句 | 状态 | 记录 |
|---|---|---|---|

状态：`open` 会说；`off` 用户说过别提醒。

（空）

## 待确认

摆给用户看过、他还没答的。

（空）

## 撤回的

用户否认的，保留不删，写明他怎么说的。

（空）
