# 这台机器

槽位层。装机时由发现器填，之后每次服务健康检查更新"最近健康"。这里只放和运行有关的事实，不放个人信息；路径写绝对路径，另一台机器有自己的一份。

<!-- dsh-state -->
```json
{"slot": "machine", "status": "empty", "machine_id": null, "os": null, "filled_by": null, "last_health_at": null, "user_version": 0, "updated_at": null}
```
<!-- /dsh-state -->

## 系统

操作系统与版本、用户目录、时区、本机时钟与网络时间的偏差。

（空）

## 工作区

框架所在路径（本文件所在目录）、runs 目录、审计文件。

（空）

## 运行环境

Node、Python 版本；DSH 版本、`DSH_HOME`、监听端口；入口 Agent 是什么、怎么连；已装的插件及版本。

（空）

## 可用工具与服务

| 工具或服务 | 用途 | 地址或命令 | 状态 | 最后核验 |
|---|---|---|---|---|
| | | | | |

## 最近健康

四类健康的最近一次结果与时间，指向 `connection/HEALTH.md` 的完整记录。定义见 `protocols/HEALTH.md`。

（空）
